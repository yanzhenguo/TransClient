spring:
    datasource:
        url: jdbc:mysql://127.0.0.1:3306/film
        username: root
        password: admin123
        driver-class-name: com.mysql.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
            show-sql: true
# 节目本地存放目录
filmDir: D:\\films
#主站的IP地址
mainStation: 127.0.0.1
# 分站的id
onlyId: 2342
# 日志记录级别
logging:
  level:
      edu: debug
# 内容服务器的端口
remotePort: 8080